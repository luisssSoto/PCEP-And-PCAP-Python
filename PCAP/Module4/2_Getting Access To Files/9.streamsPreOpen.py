"""Streams pre-open"""
'''You must to create a stream object, (open() a file) to start work with but,
there are 3 streams that you can use by importing the sys module, they work
while the programs is executing:'''

import sys

'''stdin (standard input):
read the data from the keyboard, actually an input() function reads data 
from stdin by default'''

'''stdout (standard output):
write the data on the screen while the program is executing, the print()
function send the data to the stdout stream'''

'''stderr (standard error)
send the data about errors to the screen'''
